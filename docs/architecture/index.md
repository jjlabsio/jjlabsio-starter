# Architecture

Architecture notes for durable design context.

## Current Notes

- The CLI source lives in `src/`.
- The generated project source lives in `template/`.
- Generated project setup guidance lives in `template/README.md`.

Add architecture notes here when a durable design explanation is needed.
